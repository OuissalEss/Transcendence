import { gql } from '@apollo/client';

export const CREATE_USER= gql`
  mutation createUser($input: CreateUserTestInput!) {
    createUser(input: $input) {
      id
      username
      email
	  avatarTest
	  password
	  }
    }
  }
`;
